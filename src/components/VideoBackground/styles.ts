import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  container: {
    position: 'absolute',
    // minWidth: '100%',
    // minHeight: '100%',
    // right: 0,
    // left: '50%',
    // bottom: 0,
    // transform: 'translate(-50%, 0)',
    zIndex: -1,
    width: '100%',
    height: '100%',
    objectFit: 'cover',
  }
})
